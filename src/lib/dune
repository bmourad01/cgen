(library
 (name cgen)
 (public_name cgen)
 (flags -w -32-58)
 (preprocess (pps ppx_bin_prot
                  ppx_compare
                  ppx_hash
                  ppx_sexp_conv))
 (libraries
   bitvec
   bitvec-binprot
   bitvec-order
   bitvec-sexp
   core
   core_kernel.pairing_heap
   decimal
   graphlib
   monads
   regular)
 (ocamlopt_flags -O3))
