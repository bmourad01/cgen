int g = 9;

int *p = &g;

int self_ref(void) {
  int %0;
  {
    (%0 = 1 < *p, *p = %0);
    ;
  }
  int r = %0;
  if (r)
    *p = 0;
  return *p;
}

int g2 = 5;

int *q = &g2;

int compound_self(void) {
  int %0;
  int %1;
  {
    (%0 = *q, %1 = %0 + *q, *q = %1);
    ;
    return %1;
  }
}

int incdec(void) {
  int %0;
  int %1;
  int x = 6;
  {
    (%0 = x, x = %0 + 1);
    ;
  }
  int a = %0;
  {
    (%1 = x, x = %1 + 1);
    ;
  }
  int b = %1 + 1;
  return a * 100 + b * 10 + x;
}

int chain(void) {
  int %0;
  int %1;
  int a;
  int b;
  int c;
  {
    (%0 = 7, c = %0);
    (%1 = %0, b = %1);
    a = %1;
  }
  return a + b + c;
}

int cond_assign(void) {
  int %0;
  int x = 0;
  {
    (%0 = 5, x = %0);
    ;
    if (%0)
      return x;
  }
  return -1;
}
