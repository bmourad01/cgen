static int add1(int x) {
  return x + 1;
}

static int apply(int (*f)(int), int x) {
  int %0;
  {
    %0 = f(x);
    return %0;
  }
}

int via_addr(int x) {
  int %0;
  {
    %0 = ((int (*)(int (*f)(int), int x))apply)((int (*)(int))&add1, x);
    return %0;
  }
}

int via_bare(int x) {
  int %0;
  {
    %0 = ((int (*)(int (*f)(int), int x))apply)((int (*)(int))(int (*)(int x))add1, x);
    return %0;
  }
}

int via_var(int x) {
  int %0;
  int (*p)(int) = (int (*)(int))&add1;
  {
    %0 = p(x);
    return %0;
  }
}
