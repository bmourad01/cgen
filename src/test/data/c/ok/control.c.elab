int classify(int n) {
  int %0;
  int %1;
  int %2;
  int %3;
  int r = 0;
  if (n < 0)
    r = -1;
  else if (n == 0)
    goto done;
  else
    r = 1;
  while (n > 0) {
    (%0 = n, n = %0 - 1);
    (%1 = r, r = %1 + n);
  }
  {
    int i = 0;
    for (; i < 10; %3 = i, i = %3 + 1) {
      if (i == 5)
        continue;
      (%2 = r, r = %2 + i);
    }
  }
  switch (r) {
    case 0: r = 100;
    break;
    default: r = -100;
  }
  done: return r;
}
