const char * const name = (const char *)(char *)"world";

void copy_str(char *dst, const char *src) {
  char *%0;
  const char *%1;
  char %2;
  while (1) {
    (%0 = dst, dst = %0 + 1);
    (%1 = src, src = %1 + 1);
    (%2 = *%1, *%0 = %2);
    if (!%2)
      break;
  }
}
