module foo

export function w $foo(w %x, w %y) {
@0:
  %c = slt.w %y, 0x0_w ; @3
  br %c, @1, @2
@1:
  %a3 = add.w %x, 0x1_w ; @5
  jmp @4(%a3)
@2:
  %a2 = copy.w 0x5_w ; @6
  jmp @4(%a2)
@4(%a1):
  %b = add.w %x, 0x1_w ; @7
  %d = add.w %a1, %b ; @8
  ret %d
}
